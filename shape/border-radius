border-radius
既可以接受固定值，也可以接受百分比，百分比会根据元素的宽高进行解析；
可以单独设置水平半径和垂直半径，用‘/’隔开；

根据以上特性可以创建一个自适应椭圆，如下：
{
  width：  100px;
  height:  150px;
  border-radius:50%/50%;
  }
border-radius可以设置四个不同方向的拐角，从左上角按顺时针方向排列，也可以同时设置四个方向拐角的宽高，如下：
  border-radius:10px 20p  10px 20px/5px 5px 10px 10px;
